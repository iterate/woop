import React from "react";
import styled from "styled-components";

const HeaderContainer = styled.div``;

const Header = () => <HeaderContainer>Woop Header </HeaderContainer>;

export default Header;
